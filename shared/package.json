{
  "name": "trivia-shared",
  "version": "1.0.20",
  "description": "Shared utils for trivia-backend and trivia-frontend.",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": ["/build"],
  "scripts": {
    "build": "tsc",
    "pack": "npm pack --pack-destination ./dist",
    "build:pack": "npm run build && npm run pack",
    "build:all": "echo \"TODO: Run install and build in frontend and backend.\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
